export declare class AbsPublisherConfig {
    connectionString: string;
    topicName: string;
    logInConsole: boolean;
    constructor(connectionString: string, topicName: string, logInConsole?: boolean);
}
